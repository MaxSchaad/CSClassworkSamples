#include <stdio.h>

/**
 * Use the following function header for linear search. The following method should return
 * the index where the key is found in the array. If the key is not present, return -1
 * 
 * arr[] is the array you are searching through
 * N references the size of the array
 * x is the key we are searching for
*/
int linearSearch(int arr[], int N, int x)
{



}



/**
 * Use the following function header for binary search. The following method should return
 * the index where the key is found in the array. If the key is not present, return -1
 *
 * arr[] is the array you are searching through
 * left references the index that is the left boundary of where we are looking for the key
 * right references the index that is the right boundary of where we are looking for the key
 * x is the key we are searching for
*/
int binarySearch(int arr[], int left, int right, int x)
{



}

/**
 * main for driver and testing code. 
*/
int main()
{



}
