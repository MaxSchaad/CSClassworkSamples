#include <iostream>
using namespace std;

// Use the main to test your Stack class. Make sure it implements all 
// of the requried functions correctly. I will include a couple pieces of 
// code that should work, upon implementing Stack. Uncomment them when you are ready
// to test.

int main() {


	/* Test Code Uncomment when you want to try and run tests */

	//Stack myStack(5); // Stack object Constructor

	//cout << "The size of my stack is: " << myStack.size() << endl; // Should return 0
	//cout << "Is the stack empty: " << myStack.isEmpty() << endl; // Should return true
	//cout << "The top of the stack is:" << myStack.peek() << endl; // Should handle the case of an empty stack

	//myStack.push(1);

	//cout << "The top of the stack is:" << myStack.peek() << endl; // Are you adding to the stack properly? Should return 1
	//cout << "The size of my stack is: " << myStack.size() << endl; // Should return 1
	//cout << "Is the stack empty: " << myStack.isEmpty() << endl; // Should return false

	//myStack.push(2);
	//myStack.push(3);
	//myStack.push(4);
	//myStack.push(5);

	//cout << "The top of the stack is:" << myStack.peek() << endl; // What should this return? Draw your structure out if you are unsure
	//cout << "The size of my stack is: " << myStack.size() << endl; // Should return 5
	//cout << "Is the stack empty: " << myStack.isEmpty() << endl; // Should return false


	//cout << "POP! Top element is: " << myStack.pop() << endl; // Should remove the top
	//cout << "The top of the stack is:" << myStack.peek() << endl; // Top should change accordingly
	//cout << "The size of my stack is: " << myStack.size() << endl; // Should return 4
	//cout << "Is the stack empty: " << myStack.isEmpty() << endl; // Should return false


	//myStack.pop();
	//myStack.pop();
	//myStack.pop();
	//myStack.pop();

	//myStack.pop();// Does your code handle the case of attempting to pop off an empty list?

	/* End of Test Code */


	return 0


}